---
interface Props {
  title: string;
  id: string;
}

const { title, id } = Astro.props;
---

<section id=`${id}` class=`bg-bg-300 h-full w-full px-4 flex flex-col lg:px-6`>
  <header class="w-full h-16 flex items-center">
    <h2 id="title" class="text-2xl font-bold">{title}</h2>
  </header>
  <slot />
</section>
